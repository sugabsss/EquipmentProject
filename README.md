# EquipmentProject
In this project i created a software that manages enterprise equipaments, tracking them, getting metrics about their usage and fails, the stack used is : .Net 8.0 (Backend), RabbitMQ (Queues), React (Front-End), SQL Server (Database) and Redis (Cache).
